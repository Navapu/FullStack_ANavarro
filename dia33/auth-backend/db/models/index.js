// File to centralize all models into a single file so they can be used in controllers.

export { User } from "./user.model.js";
export { Product } from "./product.model.js"
export { authUser } from './auth.model.js'